<% provide(:title, 'Sign up') %>

<% content_for :subheader do %>
  <h1>Sign up</h1>
<% end %>

<div id="new-user">
  <div>
    <%= render 'shared/error_messages', target: @user %>
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <p><%= f.submit "Create my account", class: "button primary" %></p>
      <p>or <%= link_to 'Sign in as Existing Member', signin_path %></p>
    <% end %>
  </div>
</div>